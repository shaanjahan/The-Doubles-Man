# Secrets — never commit
.env
.env.*
*.key
supabase/.env
supabase/.temp/

# OS / editor junk
.DS_Store

# Node
node_modules/

# Tester data exports (real tester emails) — keep OUT of the repo. NOT *.csv
# (that would silently swallow legit game-data CSVs); ignore the Base44 export
# files by name + a data/ dir for any exports kept locally. Pass the export path
# to scripts/seed-testers.ts via the CSV env var.
/data/
The Doublesman Data*.csv
