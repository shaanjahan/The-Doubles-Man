# Secrets — never commit
.env
.env.*
*.key
supabase/.env
supabase/.temp/

# OS / editor junk
.DS_Store

# Node
node_modules/

# Tester data exports (contain real tester emails) — pass the path via the CSV
# env var to scripts/seed-testers.ts; the file lives OUTSIDE the repo.
*.csv
